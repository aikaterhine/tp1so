#ifndef MICROWAVE_HPP
#define MICROWAVE_HPP

//#include "../src/Person.cpp"

#include <string>

#include <iostream>
#include <mutex>
#include <pthread.h>

using namespace std;

class Microwave
{
	private:

	public:
		Microwave();
		/*
    void wait(Person);
    void release(Person);
    void check();
		*/
};

#endif
